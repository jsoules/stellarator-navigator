- set FLASK_ENV=development and FLASK_APP=app.py (update to a real name)
- then "flask run" in the directory with the server app



TODO:
- Should the flask server also be serving the main app? If so, how?
- Is this CORS solution adequate?